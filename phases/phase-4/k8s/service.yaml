apiVersion: v1
kind: Service
metadata:
  name: evo-todo-service
spec:
  selector:
    app: evo-todo
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
# Placeholder Kubernetes service manifest for Phase IV
